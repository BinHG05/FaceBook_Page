route:
  receiver: dead-letter-webhook
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 2h

receivers:
  - name: dead-letter-webhook
    webhook_configs:
      - url: http://alert-webhook:8080/alerts
        send_resolved: true
